# Root module for {{ project_name }}. Replace with real infrastructure.
# Provider config and the state backend are environment-specific — wire them up
# in versions.tf (providers) and a backend block once you know your target.
