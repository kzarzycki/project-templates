#!/usr/bin/env python3
"""Smoke check: construct the MCP server and confirm it builds. No transport."""

from {{ module_name }} import build_server


def main() -> None:
    server = build_server()
    assert server is not None, "build_server() returned None"
    print("mcp server smoke: build_server() OK")


if __name__ == "__main__":
    main()
